<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mis Tickets</title>
    <link rel="stylesheet" href="<%= basePath %>/css/style.css">
</head>
<body>
    <div class="container">
        <header class="app-header">
            <h1>CAPTia</h1>
            <button class="menu-toggle" id="menuToggle">
                <span></span>
                <span></span>
                <span></span>
            </button>
            <nav id="mainNav">
                <a href="<%= basePath %>/">Escanear</a>
                <a href="<%= basePath %>/tickets" class="active">Mis Tickets</a>
                <a href="<%= basePath %>/productos">Productos</a>
            </nav>
        </header>

        <main>
            <form action="<%= basePath %>/tickets" method="GET" class="search-bar">
                <input type="text" name="search" id="searchInput" placeholder="Buscar por establecimiento, fecha..." value="<%= searchQuery %>">
            </form>

            <div id="tickets-list">
                <% if (tickets.length > 0) { %>
                    <% tickets.forEach(ticket => { %>
                        <a href="<%= basePath %>/ticket/<%= ticket.id %>"
                           class="ticket-card"
                           data-search-text="<%= (ticket.fields.Establecimiento || '').toLowerCase() %> <%= ticket.fields.Fecha || '' %>"
                           data-fecha="<%= ticket.fields.Fecha || '' %>">
                            <div class="ticket-info">
                                <span class="ticket-establecimiento"><%= ticket.fields.Establecimiento || 'Sin Establecimiento' %></span>
                                <span class="ticket-fecha"><%= ticket.fields.Fecha ? new Date(ticket.fields.Fecha).toLocaleDateString('es-ES') : 'Fecha no válida' %></span>
                                <span class="ticket-total"><%= (ticket.total || 0).toFixed(2) %> €</span>
                            </div>
                        </a>
                    <% }); %>
                <% } else { %>
                    <p class="no-tickets">Aún no has escaneado ningún ticket.</p>
                <% } %>
            </div>

            <% if (pagination.totalPages > 1) { %>
            <div class="pagination" id="pagination-controls">
                <div class="pagination-info">
                    Página <%= pagination.currentPage %> de <%= pagination.totalPages %>
                    (<%= pagination.totalTickets %> tickets en total)
                </div>
                <div class="pagination-buttons">
                    <%
                        const searchParam = searchQuery ? `&search=${encodeURIComponent(searchQuery)}` : '';
                    %>
                    <% if (pagination.hasPrevPage) { %>
                        <a href="<%= basePath %>/tickets?page=<%= pagination.currentPage - 1 %><%= searchParam %>" class="pagination-btn">
                            ← Anterior
                        </a>
                    <% } else { %>
                        <span class="pagination-btn disabled">← Anterior</span>
                    <% } %>

                    <% for (let i = 1; i <= pagination.totalPages; i++) { %>
                        <% if (i === pagination.currentPage) { %>
                            <span class="pagination-btn active"><%= i %></span>
                        <% } else if (i === 1 || i === pagination.totalPages || (i >= pagination.currentPage - 2 && i <= pagination.currentPage + 2)) { %>
                            <a href="<%= basePath %>/tickets?page=<%= i %><%= searchParam %>" class="pagination-btn"><%= i %></a>
                        <% } else if (i === pagination.currentPage - 3 || i === pagination.currentPage + 3) { %>
                            <span class="pagination-btn disabled">...</span>
                        <% } %>
                    <% } %>

                    <% if (pagination.hasNextPage) { %>
                        <a href="<%= basePath %>/tickets?page=<%= pagination.currentPage + 1 %><%= searchParam %>" class="pagination-btn">
                            Siguiente →
                        </a>
                    <% } else { %>
                        <span class="pagination-btn disabled">Siguiente →</span>
                    <% } %>
                </div>
            </div>
            <% } %>
        </main>
    </div>
    <script>
        // Mobile menu toggle
        const menuToggle = document.getElementById('menuToggle');
        const mainNav = document.getElementById('mainNav');

        if (menuToggle) {
            menuToggle.addEventListener('click', function() {
                this.classList.toggle('active');
                mainNav.classList.toggle('active');
            });
        }

        // Search submit on Enter or after delay
        const searchInput = document.getElementById('searchInput');
        let searchTimeout;

        searchInput.addEventListener('keyup', function(e) {
            // Submit immediately on Enter
            if (e.key === 'Enter') {
                this.form.submit();
                return;
            }

            // Auto-submit after 1 second of no typing
            clearTimeout(searchTimeout);
            searchTimeout = setTimeout(() => {
                this.form.submit();
            }, 1000);
        });
    </script>
</body>
</html>